const fs=require('fs'),vm=require('vm'),assert=require('node:assert/strict');
function el(){return {closest(){return null},listeners:{},children:[],value:'기존 내용',selectionStart:5,selectionEnd:5,maxLength:-1,isConnected:true,after(v){this.controls=v},append(...v){this.children.push(...v)},addEventListener(k,v){this.listeners[k]=v},setAttribute(){},setSelectionRange(){},dispatchEvent(){this.saved=true}}}
const field=el();let recognition;
class Speech {constructor(){recognition=this}start(){}stop(){this.onend()}abort(){this.onend?.()}}
const ctx={window:{SpeechRecognition:Speech,addEventListener(){}},document:{querySelectorAll(){return [field]},createElement:el,querySelector(){return el()},addEventListener(){}},MutationObserver:class{observe(){}},Event:class{},console};vm.runInNewContext(fs.readFileSync('voice.js','utf8'),ctx);
const button=field.controls.children[0];button.listeners.click({preventDefault(){}});assert.equal(recognition.lang,'ko-KR');recognition.onresult({resultIndex:0,results:[Object.assign([{transcript:'새로운 발견'}],{isFinal:true})]});assert.equal(field.value,'기존 내용 새로운 발견');assert(field.saved);recognition.onresult({resultIndex:0,results:[Object.assign([{transcript:'새로운 발견'}],{isFinal:true})]});assert.equal(field.value,'기존 내용 새로운 발견');field.listeners.beforeinput();assert.equal(button.textContent,'🎤 음성 입력');console.log('PASS: Korean recognition setup, preserve existing text, save event, duplicate result prevention, manual input cancellation');
